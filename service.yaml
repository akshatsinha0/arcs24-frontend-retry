apiVersion: v1
kind: Service
metadata:
  name: arcs24-svc
spec:
  type: NodePort
  selector:
    app: arcs24
  ports:
    - protocol: TCP
      port: 80          # the port your app listens on in the pod
      targetPort: 80
      nodePort: 30080   # exposes it on host port 30080

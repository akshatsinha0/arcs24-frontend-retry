kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 30080  # the NodePort in your Service
    hostPort: 30080       # exposed on your EC2 host
    protocol: TCP
